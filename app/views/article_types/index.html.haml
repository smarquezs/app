%h1 Listing article_types

%table
  %thead
    %tr
      %th Name
      %th Description
      %th
      %th
      %th

  %tbody
    - @article_types.each do |article_type|
      %tr
        %td= article_type.name
        %td= article_type.description
        %td= link_to 'Show', article_type
        %td= link_to 'Edit', edit_article_type_path(article_type)
        %td= link_to 'Destroy', article_type, method: :delete, data: { confirm: 'Are you sure?' }

%br

= link_to 'New Article type', new_article_type_path
